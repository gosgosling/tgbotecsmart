TELEGRAM_TOKEN=8115317558:AAEXJG6L2nt6WGA9ZWZry2eegouvyIymsvc
MANAGER_CHAT_ID=1129792166

# Режим работы: webhook/polling
# В локальной разработке используем polling (false)
# На Render должно быть true
USE_WEBHOOK=true

# Порт для веб-сервера (при использовании webhook)
# На Render порт будет автоматически установлен через переменную окружения PORT
# PORT=10000

# PostgreSQL на Render - настройка автоматическая через переменную окружения DATABASE_URL
# Примечание: URL должен начинаться с postgresql://, а не postgres://
# DATABASE_URL будет автоматически установлен Render

# URL для webhook на Render
# WEBHOOK_URL будет сформирован автоматически из RENDER_EXTERNAL_URL
# Но его можно указать явно, если нужно:
# WEBHOOK_URL=https://your-app-name.onrender.com/bot${TELEGRAM_TOKEN}

# Настройки для отладки
DEBUG=false 